import ToggleSchemeBtn from './ToggleSchemeBtn';

const Navbar = () => {
  return (
    <nav className="navbar">
      <h1>DISC-FIX</h1>
      <ToggleSchemeBtn />
    </nav>
  );
};

export default Navbar;
